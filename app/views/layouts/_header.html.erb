<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="navbar-inner">
    <div class="container">
      <%= link_to "TAMU Bulletin", home_path, id: "logo" %>
      <nav>
        <ul class="nav pull-right">
          <li><%= link_to "Home",    home_path %></li>
          <% if signed_in? %>
            <li><%= link_to "Posters", '/posters/mainlist' %></li>
            <li id="fat-menu" class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                Account <b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to "Profile", current_account %></li>
                <% if is_user? %>
                  <li><%= link_to "User Settings", edit_user_path(current_account) %></li>
                <% elsif is_organization? %>
                  <li><%= link_to "Organization Settings", edit_organization_path(current_account) %></li>
                <% end %>
                <% if admin? %>
                  <li><%= link_to "View All Users", '#' %></li>
                <% else %>
                  <li><%= link_to "Cannot View All Users", '#' %></li>
                <% end %>
                <li class="divider"></li>
                <li>
                  <%= link_to "Sign out", signout_path, method: "delete" %>
                </li>
              </ul>
            </li>
          <% else %>
            <li><%= link_to "Sign in", signin_path %></li>
          <% end %>
          <li><%= link_to "Help",    help_path %></li>
        </ul>
      </nav>
    </div>
  </div>
</header>